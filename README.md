VSCode extension to add useful links for Cargo.toml dependencies.

https://code.visualstudio.com/api/working-with-extensions/publishing-extension

Usage

```bash
$ cd repo_folder
$ vsce package
# repo_folder.vsix generated
$ vsce publish
# <publisher id>.repo_folder published to VS Code Marketplace
```
